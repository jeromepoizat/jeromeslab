import { useEffect, useState } from 'react'
import './App.css'

type ApiState = 'checking' | 'available' | 'unavailable'
type Theme = 'dark' | 'light'
type SetupStatus = 'loading' | 'needs_workspace' | 'configured' | 'workspace_unavailable' | 'unavailable'
type WorkspaceSetup = {
  recommended_workspace_path: string
  setup_token: string
  workspace_path: string | null
  workspace_state: 'unconfigured' | 'available' | 'unavailable'
  workspace_error: string | null
}

const apiStateLabels: Record<ApiState, string> = { checking: 'Checking connection', available: 'Connected', unavailable: 'Not connected' }
const healthCheckIntervalMs = 3_000

function SunIcon() { return <svg aria-hidden="true" viewBox="0 0 24 24"><circle cx="12" cy="12" r="4" /><path d="M12 2v2M12 20v2M4.93 4.93l1.42 1.42M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.42-1.42M17.66 6.34l1.41-1.41" /></svg> }
function MoonIcon() { return <svg aria-hidden="true" viewBox="0 0 24 24"><path d="M20.4 15.3A9 9 0 0 1 8.7 3.6 9 9 0 1 0 20.4 15.3Z" /></svg> }
function SettingsIcon() { return <svg aria-hidden="true" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3" /><path d="M19.4 15a1.7 1.7 0 0 0 .34 1.88l.06.06-2.12 2.12-.06-.06a1.7 1.7 0 0 0-1.88-.34 1.7 1.7 0 0 0-1.04 1.56V20.3h-3v-.08A1.7 1.7 0 0 0 10.66 18.66a1.7 1.7 0 0 0-1.88.34l-.06.06-2.12-2.12.06-.06A1.7 1.7 0 0 0 7 15a1.7 1.7 0 0 0-1.56-1.04h-.08v-3h.08A1.7 1.7 0 0 0 7 9.92a1.7 1.7 0 0 0-.34-1.88l-.06-.06 2.12-2.12.06.06a1.7 1.7 0 0 0 1.88.34 1.7 1.7 0 0 0 1.04-1.56V4.62h3v.08a1.7 1.7 0 0 0 1.04 1.56 1.7 1.7 0 0 0 1.88-.34l.06-.06 2.12 2.12-.06.06A1.7 1.7 0 0 0 19.4 9.92a1.7 1.7 0 0 0 1.56 1.04h.08v3h-.08A1.7 1.7 0 0 0 19.4 15Z" /></svg> }

function App() {
  const [apiState, setApiState] = useState<ApiState>('checking')
  const [theme, setTheme] = useState<Theme>('dark')
  const [setupStatus, setSetupStatus] = useState<SetupStatus>('loading')
  const [workspaceSetup, setWorkspaceSetup] = useState<WorkspaceSetup | null>(null)
  const [workspacePath, setWorkspacePath] = useState('')
  const [recoveryPath, setRecoveryPath] = useState('')
  const [movePath, setMovePath] = useState('')
  const [setupError, setSetupError] = useState<string | null>(null)
  const [setupNotice, setSetupNotice] = useState<string | null>(null)
  const [needsNonemptyConfirmation, setNeedsNonemptyConfirmation] = useState(false)
  const [isSelectingFolder, setIsSelectingFolder] = useState(false)
  const [isConfiguringWorkspace, setIsConfiguringWorkspace] = useState(false)
  const [isRecovering, setIsRecovering] = useState(false)
  const [isMoving, setIsMoving] = useState(false)
  const [needsMoveConfirmation, setNeedsMoveConfirmation] = useState(false)
  const [isSettingsOpen, setIsSettingsOpen] = useState(false)
  const [isForgettingWorkspace, setIsForgettingWorkspace] = useState(false)
  const [needsForgetConfirmation, setNeedsForgetConfirmation] = useState(false)

  useEffect(() => { document.documentElement.dataset.theme = theme }, [theme])
  useEffect(() => {
    let controller: AbortController | null = null; let disposed = false
    const checkHealth = async () => { controller?.abort(); controller = new AbortController(); try { const response = await fetch('/api/health', { signal: controller.signal }); if (!response.ok) throw new Error('Health request failed'); if (!disposed) setApiState('available') } catch (error: unknown) { if (!(error instanceof DOMException && error.name === 'AbortError') && !disposed) setApiState('unavailable') } }
    const checkWhenVisible = () => { if (document.visibilityState === 'visible') void checkHealth() }
    void checkHealth(); const intervalId = window.setInterval(() => void checkHealth(), healthCheckIntervalMs); document.addEventListener('visibilitychange', checkWhenVisible); window.addEventListener('focus', checkWhenVisible)
    return () => { disposed = true; window.clearInterval(intervalId); document.removeEventListener('visibilitychange', checkWhenVisible); window.removeEventListener('focus', checkWhenVisible); controller?.abort() }
  }, [])
  useEffect(() => {
    const controller = new AbortController()
    fetch('/api/setup', { signal: controller.signal }).then(async response => { if (!response.ok) throw new Error('The local setup service is unavailable.'); return (await response.json()) as WorkspaceSetup }).then(setup => { setWorkspaceSetup(setup); setWorkspacePath(setup.workspace_path ?? setup.recommended_workspace_path); setRecoveryPath(setup.workspace_path ?? ''); setSetupStatus(setup.workspace_state === 'available' ? 'configured' : setup.workspace_state === 'unavailable' ? 'workspace_unavailable' : 'needs_workspace') }).catch(error => { if (!(error instanceof DOMException && error.name === 'AbortError')) setSetupStatus('unavailable') })
    return () => controller.abort()
  }, [])

  const readApiError = async (response: Response) => ((await response.json().catch(() => null)) as { detail?: string } | null)?.detail ?? 'The request could not be completed. Try again.'
  const selectFolder = async (setPath: (path: string) => void) => { if (workspaceSetup === null) return; setIsSelectingFolder(true); setSetupError(null); try { const response = await fetch('/api/setup/select-folder', { method: 'POST', headers: { 'X-Jeromes-Lab-Setup-Token': workspaceSetup.setup_token } }); if (!response.ok) throw new Error(await readApiError(response)); const result = (await response.json()) as { path: string | null }; if (result.path !== null) setPath(result.path) } catch (error: unknown) { setSetupError(error instanceof Error ? error.message : 'The folder picker could not be opened.') } finally { setIsSelectingFolder(false) } }
  const configureWorkspace = async (confirmNonempty: boolean) => { if (workspaceSetup === null) return; setIsConfiguringWorkspace(true); setSetupError(null); try { const response = await fetch('/api/setup/workspace', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Jeromes-Lab-Setup-Token': workspaceSetup.setup_token }, body: JSON.stringify({ path: workspacePath, confirm_nonempty: confirmNonempty }) }); if (!response.ok) { const message = await readApiError(response); if (!confirmNonempty && message.includes('already contains files')) { setNeedsNonemptyConfirmation(true); setSetupError(message); return }; throw new Error(message) }; const result = (await response.json()) as { workspace_path: string }; setWorkspaceSetup({ ...workspaceSetup, workspace_path: result.workspace_path, workspace_state: 'available', workspace_error: null }); setWorkspacePath(result.workspace_path); setSetupStatus('configured'); setNeedsNonemptyConfirmation(false) } catch (error: unknown) { setSetupError(error instanceof Error ? error.message : 'The workspace could not be created.') } finally { setIsConfiguringWorkspace(false) } }
  const recoverWorkspace = async () => { if (workspaceSetup === null) return; setIsRecovering(true); setSetupError(null); try { const response = await fetch('/api/recovery/locate-workspace', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Jeromes-Lab-Setup-Token': workspaceSetup.setup_token }, body: JSON.stringify({ path: recoveryPath }) }); if (!response.ok) throw new Error(await readApiError(response)); const result = (await response.json()) as { workspace_path: string }; setWorkspaceSetup({ ...workspaceSetup, workspace_path: result.workspace_path, workspace_state: 'available', workspace_error: null }); setWorkspacePath(result.workspace_path); setSetupStatus('configured'); setSetupNotice('The existing workspace was reconnected. No data was copied or deleted.') } catch (error: unknown) { setSetupError(error instanceof Error ? error.message : 'The workspace could not be reconnected.') } finally { setIsRecovering(false) } }
  const moveWorkspace = async () => { if (workspaceSetup === null) return; setIsMoving(true); setSetupError(null); try { const response = await fetch('/api/settings/move-workspace', { method: 'POST', headers: { 'Content-Type': 'application/json', 'X-Jeromes-Lab-Setup-Token': workspaceSetup.setup_token }, body: JSON.stringify({ path: movePath }) }); if (!response.ok) throw new Error(await readApiError(response)); const result = (await response.json()) as { previous_workspace_path: string, workspace_path: string }; setWorkspaceSetup({ ...workspaceSetup, workspace_path: result.workspace_path }); setWorkspacePath(result.workspace_path); setMovePath(''); setNeedsMoveConfirmation(false); setSetupNotice(`Workspace moved to '${result.workspace_path}'. The original at '${result.previous_workspace_path}' was left untouched.`) } catch (error: unknown) { setSetupError(error instanceof Error ? error.message : 'The workspace could not be moved.') } finally { setIsMoving(false) } }
  const forgetWorkspace = async () => { if (workspaceSetup === null) return; setIsForgettingWorkspace(true); setSetupError(null); try { const response = await fetch('/api/settings/forget-workspace', { method: 'POST', headers: { 'X-Jeromes-Lab-Setup-Token': workspaceSetup.setup_token } }); if (!response.ok) throw new Error(await readApiError(response)); const result = (await response.json()) as { forgotten_workspace_path: string }; setWorkspaceSetup({ ...workspaceSetup, workspace_path: null, workspace_state: 'unconfigured', workspace_error: null }); setWorkspacePath(workspaceSetup.recommended_workspace_path); setSetupNotice(`Workspace forgotten on this device. '${result.forgotten_workspace_path}' was not deleted.`); setSetupStatus('needs_workspace'); setIsSettingsOpen(false); setNeedsForgetConfirmation(false) } catch (error: unknown) { setSetupError(error instanceof Error ? error.message : 'The workspace could not be forgotten.') } finally { setIsForgettingWorkspace(false) } }
  const nextTheme = theme === 'dark' ? 'light' : 'dark'
  return <div className="app-shell"><header className="app-header"><div><p className="eyebrow">Local scientific workspace</p><h1>Jerome's Laboratory</h1></div><div className="header-actions"><div className={`api-status api-status--${apiState}`} role="status" aria-label={apiStateLabels[apiState]} tabIndex={0}><span className="api-status-dot" aria-hidden="true" /><span className="api-status-tooltip" role="tooltip">{apiStateLabels[apiState]}</span></div><button className="theme-toggle" type="button" aria-label={`Switch to ${nextTheme} mode`} title={`Switch to ${nextTheme} mode`} onClick={() => setTheme(nextTheme)}>{theme === 'dark' ? <SunIcon /> : <MoonIcon />}</button>{setupStatus === 'configured' && <button className="theme-toggle" type="button" aria-label="Open settings" aria-controls="settings-drawer" aria-expanded={isSettingsOpen} title="Settings" onClick={() => setIsSettingsOpen(!isSettingsOpen)}><SettingsIcon /></button>}</div></header><main>{setupStatus === 'loading' && <section className="foundation-card"><p className="step-label">Preparing workspace</p><h2>Checking local setup</h2><p>Jerome&apos;s Laboratory is checking its local configuration.</p></section>}{setupStatus === 'needs_workspace' && <section className="foundation-card workspace-setup"><p className="step-label">First-run setup</p><h2>Choose your research workspace</h2><p>Your projects, local database, artifacts, exports, and backups stay together in this folder. You can paste a path or select a folder from your computer.</p>{setupNotice && <p className="setup-notice">{setupNotice}</p>}<form onSubmit={event => { event.preventDefault(); void configureWorkspace(false) }}><label htmlFor="workspace-path">Workspace folder</label><div className="workspace-path-controls"><input id="workspace-path" value={workspacePath} onChange={event => { setWorkspacePath(event.target.value); setNeedsNonemptyConfirmation(false) }} required /><button className="secondary-button" type="button" onClick={() => void selectFolder(setWorkspacePath)} disabled={isSelectingFolder || isConfiguringWorkspace}>{isSelectingFolder ? 'Opening…' : 'Select folder'}</button></div>{setupError && <p className="setup-error">{setupError}</p>}{needsNonemptyConfirmation && <button className="secondary-button" type="button" onClick={() => void configureWorkspace(true)}>Use this folder anyway</button>}<button className="primary-button" type="submit" disabled={isConfiguringWorkspace}>{isConfiguringWorkspace ? 'Preparing workspace…' : 'Continue'}</button></form></section>}{setupStatus === 'workspace_unavailable' && <section className="foundation-card workspace-setup"><p className="step-label">Workspace recovery</p><h2>Locate your moved workspace</h2><p>{workspaceSetup?.workspace_error ?? 'The saved workspace is unavailable.'}</p><p>Choose the existing Jerome&apos;s Laboratory folder. This reconnects it; it does not create, copy, or delete data.</p><div className="workspace-path-controls"><input value={recoveryPath} onChange={event => setRecoveryPath(event.target.value)} aria-label="Existing workspace folder" required /><button className="secondary-button" type="button" onClick={() => void selectFolder(setRecoveryPath)} disabled={isSelectingFolder || isRecovering}>{isSelectingFolder ? 'Opening…' : 'Select folder'}</button></div>{setupError && <p className="setup-error">{setupError}</p>}<div className="settings-confirmation"><button className="primary-button" type="button" onClick={() => void recoverWorkspace()} disabled={isRecovering}>{isRecovering ? 'Reconnecting…' : 'Locate workspace'}</button><button className="text-button" type="button" onClick={() => void forgetWorkspace()} disabled={isForgettingWorkspace}>{isForgettingWorkspace ? 'Forgetting…' : 'Forget saved location'}</button></div></section>}{setupStatus === 'configured' && <section className="foundation-card"><p className="step-label">Local workspace ready</p><h2>Application foundation</h2><p>Your workspace is ready at <code>{workspacePath}</code>. Project and scientific workflow features will be added incrementally with provenance preserved from the start.</p>{setupNotice && <p className="setup-notice">{setupNotice}</p>}</section>}{setupStatus === 'unavailable' && <section className="foundation-card"><p className="step-label">Local setup unavailable</p><h2>Reconnect the local application</h2><p>Start Jerome&apos;s Laboratory again, then refresh this page.</p></section>}</main>{isSettingsOpen && setupStatus === 'configured' && <aside className="settings-drawer" id="settings-drawer" aria-labelledby="settings-title"><div className="settings-drawer-header"><div><p className="step-label">Settings</p><h2 id="settings-title">Local configuration</h2></div><button className="settings-close" type="button" aria-label="Close settings" onClick={() => { setIsSettingsOpen(false); setNeedsForgetConfirmation(false); setNeedsMoveConfirmation(false) }}>×</button></div><p>This device remembers the workspace below so later launches reopen it.</p><code className="workspace-location">{workspacePath}</code><div className="settings-section"><h3>Move workspace</h3><p>Copy to a new or empty folder, verify every file, then switch this device to the copy. The original folder stays untouched.</p><div className="workspace-path-controls"><input value={movePath} onChange={event => { setMovePath(event.target.value); setNeedsMoveConfirmation(false) }} placeholder="New empty destination folder" aria-label="New workspace destination" /><button className="secondary-button" type="button" onClick={() => void selectFolder(setMovePath)} disabled={isSelectingFolder || isMoving}>{isSelectingFolder ? 'Opening…' : 'Select folder'}</button></div>{needsMoveConfirmation ? <div className="settings-confirmation"><p>Copy and verify the workspace now? The original will not be deleted.</p><button className="primary-button" type="button" onClick={() => void moveWorkspace()} disabled={isMoving}>{isMoving ? 'Copying and verifying…' : 'Confirm move workspace'}</button><button className="text-button" type="button" onClick={() => setNeedsMoveConfirmation(false)}>Cancel</button></div> : <button className="secondary-button" type="button" onClick={() => setNeedsMoveConfirmation(true)} disabled={!movePath.trim()}>Move workspace</button>}</div><div className="settings-danger-zone"><h3>Testing and recovery</h3><p>Forgetting this workspace removes only this device&apos;s saved location. It does not delete the database, projects, artifacts, or folder.</p>{needsForgetConfirmation ? <div className="settings-confirmation"><p>Continue? You can select this same folder later to reopen it.</p><button className="secondary-button" type="button" onClick={() => void forgetWorkspace()} disabled={isForgettingWorkspace}>{isForgettingWorkspace ? 'Forgetting…' : 'Confirm forget workspace'}</button><button className="text-button" type="button" onClick={() => setNeedsForgetConfirmation(false)}>Cancel</button></div> : <button className="secondary-button" type="button" onClick={() => setNeedsForgetConfirmation(true)}>Forget workspace on this device</button>}{setupError && <p className="setup-error">{setupError}</p>}</div></aside>}</div>
}

export default App
