# Test suite

Automated tests will live here. `docs/ROADMAP.md` lists the behavior that each
milestone must cover. External LLM and scientific APIs must be mocked in normal
tests; live calls must never be required for the default suite.

